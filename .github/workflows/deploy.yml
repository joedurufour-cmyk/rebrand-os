name: Deploy REBRAND.OS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Trigger Render Deploy
        run: |
          echo "Push to main detected — Render auto-deploys via GitHub integration"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref_name }}"
